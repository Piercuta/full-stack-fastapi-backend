# Copy to secrets.env and fill in. Do not commit secrets.env.
# Postgres password comes from Secrets Manager via AWS_SECRET_ARN_SSM_PARAM in generated-config.env.
FIRST_SUPERUSER_PASSWORD=REPLACE_ME
SECRET_KEY=REPLACE_ME
